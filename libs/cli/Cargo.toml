[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
webrtc = { workspace = true }

shellwords = "1.1.0"
