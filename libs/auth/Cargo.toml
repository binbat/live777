[package]
name = "auth"
edition.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
headers = "0.4.0"
http = "1.0.0"
http-body = "1.0.0"
tower-http = { version = "0.5.2", features = ["validate-request"] }
