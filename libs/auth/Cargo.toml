[package]
name = "auth"
edition.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
http = { workspace = true }
http-body = { workspace = true }

headers = "0.4.0"
tower-http = { version = "0.5.2", features = ["validate-request"] }
