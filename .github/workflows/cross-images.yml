name: Cross Images

on:
  push:
    branches:
      - main
      - test
      - docker
    tags:
      - 'v*'
    paths:
      - '.github/workflows/cross-images.yml'
      - 'docker/Dockerfile.cross-aarch64-rkmpp'
      - 'docker/Dockerfile.cross-aarch64-rpi'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: ${{ matrix.name }} cross image
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Rockchip RKMPP
            dockerfile: docker/Dockerfile.cross-aarch64-rkmpp
            image: crossbuilder-aarch64-rkmpp
          - name: Raspberry Pi
            dockerfile: docker/Dockerfile.cross-aarch64-rpi
            image: crossbuilder-aarch64-rpi
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/binbat/${{ matrix.image }}
          tags: |
            type=raw,value=latest
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
